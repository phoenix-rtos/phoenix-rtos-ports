diff -ruN a/include/X11/Xos_r.h b/include/X11/Xos_r.h
--- a/include/X11/Xos_r.h	2024-07-11 17:36:05.660187032 +0200
+++ b/include/X11/Xos_r.h	2024-07-12 09:29:55.822006293 +0200
@@ -307,7 +307,7 @@
   (_Xos_processUnlock), \
   (p).pwp )
 
-#elif !defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(__APPLE__)
+#elif !defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(__APPLE__) && !defined(__phoenix__)
 /* SVR4 threads, AIX 4.2.0 and earlier and OSF/1 3.2 and earlier pthreads */
 # define X_NEEDS_PWPARAMS
 typedef struct {
@@ -391,6 +391,10 @@
 #include <sys/param.h>
 #endif
 
+#ifdef __phoenix__
+#define MAXHOSTNAMELEN HOST_NAME_MAX
+#endif
+
 typedef struct {
   struct hostent hent;
   char           h_name[MAXHOSTNAMELEN];
