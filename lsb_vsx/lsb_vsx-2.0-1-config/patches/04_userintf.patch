--- lsb_vsx_cp2/LSB.tools/userintf.c	2023-03-07 11:54:08.340137621 +0100
+++ lsb_vsx_cp/LSB.tools/userintf.c	2023-06-27 17:06:52.548697391 +0200
@@ -17,7 +17,11 @@
  *
  */
 
-#ifdef __linux__
+#ifndef __phoenix__
+#define __phoenix__
+#endif
+
+#if (defined(__linux__) || defined(__phoenix))
 
 /* modify the following for different mountable filesystem types */
 
@@ -430,7 +434,7 @@
  *	It returns 0 for success, -1 for failure.
  */
 
-#ifdef __linux__
+#if (defined(__linux__) || defined(__phoenix))
 #include <sys/mount.h>
 #endif
 
@@ -441,7 +445,7 @@
 {
 	int ret;
 
-#ifdef __linux__
+#if (defined(__linux__) || defined(__phoenix))
         /* file system independent mount for filesystem */
         ret = mount(spec, dir, (char *) __USERINTF_FSTYPE, MS_MGC_VAL, 0);
 #else
@@ -451,7 +455,7 @@
 	{
 		(void) unmnt(spec, dir);
 		errno = 0;
-#ifdef __linux__
+#if (defined(__linux__) || defined(__phoenix))
         /* file system independent mount for filesystem */
         ret = mount(spec, dir, __USERINTF_FSTYPE, MS_MGC_VAL, 0);
 #else
@@ -475,7 +479,7 @@
 {
 	int ret;
 
-#ifdef __linux__
+#if (defined(__linux__) || defined(__phoenix))
         /* file system independent mount for filesystem */
         ret = mount(spec, dir, __USERINTF_FSTYPE, MS_MGC_VAL|MS_RDONLY, 0);
 #else
@@ -485,7 +489,7 @@
 	{
 		(void) unmnt(spec, dir);
 		errno = 0;
-#ifdef __linux__
+#if (defined(__linux__) || defined(__phoenix))
         ret = mount(spec, dir, __USERINTF_FSTYPE, MS_MGC_VAL|MS_RDONLY, 0);
 #else
 		ret = mount(spec, dir, 1);
